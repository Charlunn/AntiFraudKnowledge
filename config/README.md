# Configuration

Copy `.env.example` to `.env` and adjust the values for development. For production, copy `.env.production.example` to `.env.production` and update the secrets accordingly. These files provide configuration for PostgreSQL, Neo4j and the DashScope (Ali Bailian) API key.
