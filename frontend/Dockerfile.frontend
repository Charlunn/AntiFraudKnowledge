# 使用 Node 18 作为基础镜像（更稳定）
FROM node:20

# 设置工作目录
WORKDIR /app

# 复制 package.json 和 package-lock.json
COPY package.json package-lock.json* ./

# 使用 npm 安装依赖（包括 devDependencies）
RUN npm install

# 再复制项目代码
COPY . .

# 暴露 Nuxt 默认端口
EXPOSE 3000

# 启动 Nuxt 开发服务器
CMD ["npm", "run", "dev"]
